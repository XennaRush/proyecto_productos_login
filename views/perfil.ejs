<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Perfil</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card p-4 text-center">
          <h3>Perfil de <%= usuario.nombre %></h3>
          <div class="my-3">
            <img src="/images/<%= usuario.avatar %>" alt="avatar" class="rounded-circle" style="width:150px;height:150px;object-fit:cover;">
            <% if (usuario.avatar !== 'default.png') { %>
              <form action="/auth/perfil/editar" method="POST" style="display:inline-block;" enctype="multipart/form-data">
                <input type="hidden" name="borrarAvatar" value="1">
                <button class="btn btn-sm btn-outline-danger mt-2">Borrar foto</button>
              </form>
            <% } %>
          </div>
          <p><strong>Usuario:</strong> <%= usuario.usuario %></p>
          <div class="d-flex justify-content-center gap-2">
            <a href="/auth/perfil/editar" class="btn btn-outline-primary">Editar perfil</a>
            <form action="/auth/perfil/eliminar" method="POST" onsubmit="return confirm('¿Eliminar cuenta? Esta acción es irreversible.')">
              <button class="btn btn-danger">Eliminar cuenta</button>
            </form>
            <a class="btn btn-close ms-2" href="/usuario" title="Cancelar" style="margin-left:8px;"></a>
          </div>
          <div class="mt-3">
            <a href="/auth/logout">Cerrar sesión</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

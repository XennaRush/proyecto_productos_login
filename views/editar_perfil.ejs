<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Editar perfil</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card p-4">
          <h3>Editar perfil</h3>
          <form action="/auth/perfil/editar" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
              <input name="nombre" class="form-control" value="<%= usuario.nombre %>" required>
            </div>
            <div class="mb-3">
              <input name="usuario" class="form-control" value="<%= usuario.usuario %>" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Foto actual</label>
              <div class="mb-2">
                <img src="/images/<%= usuario.avatar %>" alt="avatar" class="rounded-circle" style="width:100px;height:100px;object-fit:cover;">
              </div>
              <label class="form-label">Cambiar foto (opcional)</label>
              <input name="avatar" type="file" class="form-control" accept="image/*">
              <% if (usuario.avatar !== 'default.png') { %>
                <button type="button" class="btn btn-sm btn-outline-danger mt-2" onclick="document.getElementById('borrarAvatar').value = '1'; this.form.submit();">Borrar foto actual</button>
                <input type="hidden" id="borrarAvatar" name="borrarAvatar" value="">
              <% } %>
            </div>
            <div class="d-flex gap-2">
              <a class="btn btn-outline-secondary" href="/auth/perfil">Cancelar</a>
              <button type="submit" class="btn btn-primary">Guardar cambios</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
